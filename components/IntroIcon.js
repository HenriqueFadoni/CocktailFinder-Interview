import React from 'react';
import {
    StyleSheet,
    View
} from 'react-native';

const IntroIcon = () => (
    <View>
    </View>
);

const styles = StyleSheet.create({
    icon: {
        marginLeft: 35,
        color: 'white'
    }
});

export default IntroIcon;